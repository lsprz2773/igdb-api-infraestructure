services:
  frontend:
    build:
      context: https://github.com/lsprz2773/igdb-api-frontend.git#main
      dockerfile: Dockerfile
    container_name: igdb-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
