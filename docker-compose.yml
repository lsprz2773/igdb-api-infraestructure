services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: igdb-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
